# Adversarial test — broken instruction file

This file is intentionally named without the `.instructions.md` suffix
to trigger Check A of the org-config lint workflow (instruction integrity).

The lint requires all files in `.github/instructions/` to match the
`*.instructions.md` naming convention. This file violates that rule.

Breakage B — see PR description for full context.
